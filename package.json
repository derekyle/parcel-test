{
  "scripts": {
    "serve": "parcel index.html",
    "watch": "parcel watch index.html --public-url /dist/",
    "build": "parcel build index.html --public-url /dist/ --log-level verbose"
  },
  "dependencies": {
    "parcel": "^2.0.0-rc.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "rsuite": "^5.0.0"
  }
}
